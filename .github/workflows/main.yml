on:
  push:
    branches:
      - master

jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Install Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 16.x
    - run: npm install
    - run: pwd
    - run: xvfb-run -a npm run compile
      if: runner.os == 'Linux'
    - run: npm run compile
      if: runner.os != 'Linux'
    - uses: actions/upload-artifact@v3.1.3
      if: runner.os == 'MacOS'
      with:
        name: extension.vsix
        path: /Users/runner/work/Studio/Studio/venticordstudio*.vsix
    - uses: actions/upload-artifact@v3.1.3
      if: runner.os == 'Linux'
      with:
        name: extension.vsix
        path: /home/runner/work/Studio/Studio/venticordstudio*.vsix
    - uses: actions/upload-artifact@v3.1.3
      if: runner.os == 'Windows'
      with:
        name: extension.vsix
        path: D:\a\Studio\Studio\venticordstudio*.vsix
